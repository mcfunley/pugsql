#!/bin/bash

set -euo pipefail

poetry run pytest
poetry run flake8
poetry build
poetry install
bin/checkrelease.py
git tag -a v$(bin/printversion) -m "Tagging release $(bin/printversion)"
git push
git push --tags
poetry publish
